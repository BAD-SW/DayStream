{
  "buildCommand": "npm run build --workspace=packages/shared && cd packages/client && npx vite build",
  "outputDirectory": "packages/client/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
